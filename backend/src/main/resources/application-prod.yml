app:
  cors:
    # Deploy with CORS_ALLOWED_ORIGINS=https://app.example.com,https://www.example.com
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
  share:
    base-url: ${SHARE_BASE_URL}

# PostgreSQL 도입 시 추가
# spring:
#   datasource:
#     url: ${DB_URL}
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#   jpa:
#     hibernate:
#       ddl-auto: validate
